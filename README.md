# Hello World Project
This is a simple Node/Express project to serve a static HTML page.
## Installation
1. Clone the repository
2. Run `npm install`
3. Run `npm start`
## Usage
Open a web browser and navigate to `http://localhost:3000`